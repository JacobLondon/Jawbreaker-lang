
fn main() begin

    -- built in structures?
    var dll = DoublyLinkedList(head=5);
    var lst = List([1, 2, 3, 4]);
    var tple = Tuple([1, 2, 3, 4]);
    var dictionary = Dict();
    var q = Queue();
    var d = Deque();
    var s = Stack();
    var t = Tree();

    -- also able to utilize python built-in functions

end
